# Repository workflow

User direction confirmed on 9 September 2026:

- Implement and publish work in `ASWATHHARISH/nestline`.
- Use `origin` for that repository and as the default push destination.
- `kajalchourasia-cmd/nestline` is the upstream reference repository.
- Do not push to Kajal's repository, create pull requests against it, or update
  branches connected to its existing pull requests unless Aswath explicitly asks.
- The earlier plan pull request (#1) is historical and remains unchanged unless
  Aswath requests a change. New implementation work uses a separate branch.
- Use VS Code for the shared working repository.

These publication rules do not change the full canonical architecture or the
confirmed product/UI/evaluation and implementation responsibilities.
